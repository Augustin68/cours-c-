#ifndef UTILS_HPP
#define UTILS_HPP

#include <iostream>
#include <limits>
#include "../generic/grid/position.hpp"

int readInt();
bool parsePositionInput(std::string input, Position& pos);

#endif // UTILS_HPP